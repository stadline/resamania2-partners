meta {
  name: retrieve products
  type: http
  seq: 1
}

get {
  url: https://{{host}}/{{environment}}/{{client_token}}/products
  body: none
  auth: none
}

vars:post-response {
  productId: res.body["hydra:member"]?.[1]?.["@id"]
  offerId: res.body["hydra:member"]?.[1]?.offers?.[0]?.["@id"]
}
