meta {
  name: Scenarios
}

docs {
  Authentication is performed automatically via the pre-request scripts on the collection.
  
  Some requests use pre-request, post-request, or `vars` to automatically populate environment variables that are useful for sending requests.
}
