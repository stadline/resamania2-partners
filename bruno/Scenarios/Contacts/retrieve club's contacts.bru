meta {
  name: retrieve club's contacts
  type: http
  seq: 3
}

get {
  url: https://{{host}}/{{environment}}/{{client_token}}/contacts?clubId={{authClubId}}
  body: none
  auth: none
}

params:query {
  clubId: {{authClubId}}
}

headers {
  Authorization: {{jwtToken}}
  x-user-club-id: {{authClubId}}
}

vars:post-response {
  contactId: res.body["hydra:member"]?.[0]?.["@id"]
}
