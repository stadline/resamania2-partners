meta {
  name: Validate migration request
  type: http
  seq: 5
}

post {
  url: https://{{host}}/{{environment}}/{{contactMigrationRequestId}}/transitions
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{jwtToken}}
  x-user-network-node-id: {{clubOneNetworkNodeId}}
  ~x-user-club-id: {{clubOneId}}
}

body:json {
  {
      "transition": "validate",
      "code": "949875"
  }
}

body:multipart-form {
  grant_type: client_credentials
  client_id: {{clientId}}
  client_secret: {{clientSecret}}
}

vars:post-response {
  resultTargetId: res.body?.["resultTargetId"]
}
