meta {
  name: Contact the attendees of a class event by text message- email or both-
  type: http
  seq: 8
}

auth:bearer {
  token: {{JwtToken}}
}

post {
  url: https://{{host}}/{{environment}}/{{client_token}}/class_events/:id/contact
  body: none
  auth:bearer
}

params:path {
  id: 
  id: 
  client_token: 
}
