meta {
  name: Add an Offer
  type: http
  seq: 2
}

auth:bearer {
  token: {{JwtToken}}
}

post {
  url: https://{{host}}/{{environment}}/{{client_token}}/offers
  body: none
  auth:bearer
}

params:path {
  client_token: 
}
