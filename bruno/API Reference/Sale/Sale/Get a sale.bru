meta {
  name: Get a sale
  type: http
  seq: 5
}

get {
  url: https://{{host}}/{{environment}}/{{client_token}}/sales/3562572
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JwtToken}}
}

vars:post-response {
  saleId: res.body?.["@id"]
  saleAmount: res.body?.totalTI
}
